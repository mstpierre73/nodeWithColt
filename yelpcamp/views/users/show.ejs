<% include ../partials/header %>

<div class="container">
	<div class="row">
		<div class="col-md-4">
			<h1><%= user.firstName %>  <%= user.lastName %></h1>
			<div class="thumbnail">
				<img src="<%= user.avatar %>" alt="image of the user">
				<caption><h4>Courriel: <a href="mailto: <%= user.email %>"><%= user.email %></a></h4></caption>
			</div>
		</div>
	
		<div class="col-md-8">
			<ul>
				<h3>Campings publiés par : </h3>
				<h4>Nom d'usager: <%= user.username %></h4>
				<div class="thumbnail">
					<% campgrounds.forEach( (campground)=>{ %>
					<a href="/index/<%= campground.id %>"><%= campground.name %></a>
					<img src="<%= campground.image %>" alt="image of the campground">
					<% }); %>
				</div>
			</ul>
		</div>		
	</div>
</div>

<% include ../partials/footer %>